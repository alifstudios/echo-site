# Installation

```
$ helm upgrade saeedfamily echo-site --install --atomic --wait --repo https://alifstudios.github.io/echo-site --set name=alifstudios --set namespace=alifstudios --set domain=alifstudios.com
```

